// A source module target needs at least one Swift file to exist. This
// fixture's only purpose is to opt into SwiftQLSQLiteBuildValidationPlugin
// via its target directory's manifest/snapshot files (see Package.swift).
enum ValidatedLibrary {}
